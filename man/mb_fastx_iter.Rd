% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/native.R
\name{mb_fastx_iter}
\alias{mb_fastx_iter}
\alias{mb_fastx_next}
\alias{mb_fastx_n}
\alias{mb_map_fastx_batch}
\title{Iterate FASTA/FASTQ batches without R sequence materialization}
\usage{
mb_fastx_iter(
  path,
  paired = c("auto", "none", "by_name", "two_file"),
  batch_bases = 100000000L,
  max_batch_bases = batch_bases,
  with_qual = FALSE,
  with_comment = FALSE
)

mb_fastx_next(iter)

mb_fastx_n(batch)

mb_map_fastx_batch(batch, index, opt = mb_opts())
}
\arguments{
\item{path}{One FASTA/FASTQ path, or two paths for \code{paired = "two_file"}.}

\item{paired}{Pairing mode.}

\item{batch_bases}{Approximate number of sequence bases to read per batch.}

\item{max_batch_bases}{Maximum bases to read when extending a batch to keep a
short paired fragment together.}

\item{with_qual, with_comment}{Preserve qualities/comments in the native batch.}

\item{iter}{Native FASTX iterator returned by \code{mb_fastx_iter()}.}

\item{batch}{Native FASTX batch returned by \code{mb_fastx_next()}.}

\item{index}{Index handle returned by \code{mb_index_load()}.}

\item{opt}{Options list from \code{mb_opts()}.}
}
\value{
\code{mb_fastx_iter()} returns a native iterator. \code{mb_fastx_next()} returns
a native FASTX batch or \code{NULL} at EOF. \code{mb_fastx_n()} returns the number of
records in a FASTX batch. \code{mb_map_fastx_batch()} returns a native alignment
batch.
}
\description{
\code{mb_fastx_iter()} opens one FASTA/FASTQ file, or two mate files, using the
vendored minibwa reader. \code{mb_fastx_next()} returns a native FASTX batch, and
\code{mb_map_fastx_batch()} maps that batch directly from native sequence buffers.
}
\details{
Pairing modes follow the CLI reader shape: \code{"none"} treats each record as a
fragment, \code{"by_name"} groups adjacent records in one file with matching query
names, and \code{"two_file"} reads one record from each of two files per fragment.
\code{"auto"} selects \code{"two_file"} for two paths and \code{"none"} for one path.
}
