% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/native.R
\name{mb_map_batch}
\alias{mb_map_batch}
\title{Map a batch of query sequences with native minibwa}
\usage{
mb_map_batch(x, index, opt = mb_opts(), name = NULL)
}
\arguments{
\item{x}{Character vector or list of raw vectors / character scalars.}

\item{index}{Index handle returned by \code{mb_index_load()}.}

\item{opt}{Options list from \code{mb_opts()}.}

\item{name}{Optional character vector of read names, or a list of raw vectors
/ character scalars. If supplied, it must have the same length as \code{x}.}
}
\value{
A native alignment batch external pointer.
}
\description{
\code{mb_map_batch()} maps many reads in one native call and returns the same
columnar alignment-batch object as \code{mb_map()}. Input may be a character
vector of sequence bytes or a list whose elements are raw vectors or
character scalars. Unlike the CLI reader, this direct batch API defaults to
unpaired mapping unless \code{opt = mb_opts(paired = TRUE)}. In paired mode, order
records as \verb{R1, R2, R1, R2, ...}; this call pairs adjacent records and does
not group FASTQ records by name for you.
}
