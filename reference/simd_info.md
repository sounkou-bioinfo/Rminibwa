# Report SIMD dispatch diagnostics

Report SIMD dispatch diagnostics

## Usage

``` r
simd_info()
```

## Value

A named list with dispatch mode, requested backend, selected backend,
compiled backends, CPU-supported backends, available backends, and
target.
